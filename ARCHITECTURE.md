# Architecture and Workflow Diagrams

## System Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                          Local Development Machine                   │
│                                                                       │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │                    Apache Airflow (2.9.3)                     │   │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐       │   │
│  │  │  Webserver   │  │  Scheduler   │  │   Database   │       │   │
│  │  │  (Port 8080) │  │  (Triggers   │  │   (SQLite)   │       │   │
│  │  │              │  │   DAGs)      │  │              │       │   │
│  │  └──────────────┘  └──────────────┘  └──────────────┘       │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                              │                                       │
│                              │ Triggers                              │
│                              ▼                                       │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │              Airflow DAG: hello_world_spark_airflow         │   │
│  │                                                               │   │
│  │   ┌──────────────┐                                           │   │
│  │   │ Start Task   │  (PythonOperator)                        │   │
│  │   │ Initialize   │  - Log start time                        │   │
│  │   └──────┬───────┘  - Set up context                        │   │
│  │          │                                                   │   │
│  │          ▼                                                   │   │
│  │   ┌──────────────┐                                           │   │
│  │   │  Validate    │  (PythonOperator)                        │   │
│  │   │ Environment  │  - Check input files exist               │   │
│  │   └──────┬───────┘  - Verify Spark job script              │   │
│  │          │          - Ensure output dir is writable         │   │
│  │          ▼                                                   │   │
│  │   ┌──────────────┐                                           │   │
│  │   │  Run Spark   │  (BashOperator)                          │   │
│  │   │    Job       │  - Execute hello_world_spark.py          │   │
│  │   └──────┬───────┘  - Process data                          │   │
│  │          │                                                   │   │
│  │          ▼                                                   │   │
│  │   ┌──────────────┐                                           │   │
│  │   │  Validate    │  (PythonOperator)                        │   │
│  │   │   Output     │  - Check output files created            │   │
│  │   └──────┬───────┘  - Verify data integrity                │   │
│  │          │                                                   │   │
│  │          ▼                                                   │   │
│  │   ┌──────────────┐                                           │   │
│  │   │  End Task    │  (PythonOperator)                        │   │
│  │   │  Finalize    │  - Log completion                        │   │
│  │   └──────────────┘  - Cleanup                               │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                              │                                       │
│                              │ Invokes                               │
│                              ▼                                       │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │                Apache Spark (3.5.1) - Local Mode             │   │
│  │  ┌──────────────────────────────────────────────────────┐   │   │
│  │  │              Spark Job: hello_world_spark.py         │   │   │
│  │  │                                                        │   │   │
│  │  │  Step 1: Read Data                                    │   │   │
│  │  │  ┌────────────────────────────────┐                  │   │   │
│  │  │  │ spark.read.csv()               │                  │   │   │
│  │  │  │ - Read sample_sales_data.csv   │                  │   │   │
│  │  │  │ - Infer schema                 │                  │   │   │
│  │  │  │ - 15 records                   │                  │   │   │
│  │  │  └────────────────────────────────┘                  │   │   │
│  │  │          │                                            │   │   │
│  │  │          ▼                                            │   │   │
│  │  │  Step 2: Transform Data                              │   │   │
│  │  │  ┌────────────────────────────────┐                  │   │   │
│  │  │  │ df.withColumn()                │                  │   │   │
│  │  │  │ - Calculate total_amount       │                  │   │   │
│  │  │  │ - Filter amount > 100          │                  │   │   │
│  │  │  │ - Select columns               │                  │   │   │
│  │  │  └────────────────────────────────┘                  │   │   │
│  │  │          │                                            │   │   │
│  │  │          ▼                                            │   │   │
│  │  │  Step 3: Aggregate Data                              │   │   │
│  │  │  ┌────────────────────────────────┐                  │   │   │
│  │  │  │ DataFrame API:                 │                  │   │   │
│  │  │  │ - groupBy + agg                │                  │   │   │
│  │  │  │                                │                  │   │   │
│  │  │  │ Spark SQL:                     │                  │   │   │
│  │  │  │ - createOrReplaceTempView      │                  │   │   │
│  │  │  │ - spark.sql()                  │                  │   │   │
│  │  │  └────────────────────────────────┘                  │   │   │
│  │  │          │                                            │   │   │
│  │  │          ▼                                            │   │   │
│  │  │  Step 4: Write Output                                │   │   │
│  │  │  ┌────────────────────────────────┐                  │   │   │
│  │  │  │ df.write.csv()                 │                  │   │   │
│  │  │  │ - transformed_data             │                  │   │   │
│  │  │  │ - sales_by_category            │                  │   │   │
│  │  │  │ - avg_by_region                │                  │   │   │
│  │  │  │ - top_customers                │                  │   │   │
│  │  │  └────────────────────────────────┘                  │   │   │
│  │  └──────────────────────────────────────────────────────┘   │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                              │                                       │
│                              ▼                                       │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │                      File System                              │   │
│  │                                                               │   │
│  │  Input:  data/input/sample_sales_data.csv                    │   │
│  │  Output: data/output/                                        │   │
│  │          - transformed_data_YYYYMMDD_HHMMSS/                 │   │
│  │          - sales_by_category_YYYYMMDD_HHMMSS/                │   │
│  │          - avg_by_region_YYYYMMDD_HHMMSS/                    │   │
│  │          - top_customers_YYYYMMDD_HHMMSS/                    │   │
│  └─────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
```

## Data Flow Diagram

```
┌─────────────┐
│   Input     │
│   CSV File  │  (15 transaction records)
│  (Sales     │  - transaction_id, customer_id
│   Data)     │  - product_name, category
└──────┬──────┘  - quantity, unit_price
       │         - transaction_date, region
       │
       ▼
┌─────────────────────────────────────────────────────┐
│         Spark: Read CSV                             │
│  df = spark.read.csv(path, header=True)             │
└──────────────────┬──────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────┐
│    Transformation 1: Add Calculated Column          │
│  df = df.withColumn("total_amount",                 │
│                     quantity * unit_price)          │
└──────────────────┬──────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────┐
│    Transformation 2: Filter Data                    │
│  df = df.filter(col("total_amount") > 100)          │
│  (Keeps only high-value transactions)               │
└──────────────────┬──────────────────────────────────┘
                   │
                   ├─────────────────────────────────────┐
                   │                                     │
                   ▼                                     ▼
   ┌───────────────────────────┐         ┌──────────────────────────┐
   │   Aggregation Path 1:     │         │  Aggregation Path 2:     │
   │   DataFrame API           │         │  Spark SQL               │
   │                           │         │                          │
   │  sales_by_category =      │         │  CREATE TEMP VIEW sales  │
   │    df.groupBy("category") │         │                          │
   │      .agg(                │         │  SELECT region,          │
   │        sum("total"),      │         │    AVG(total_amount),    │
   │        count("*")         │         │    COUNT(*),             │
   │      )                    │         │    SUM(total_amount)     │
   │                           │         │  FROM sales              │
   │  Result:                  │         │  GROUP BY region         │
   │  - Electronics: $X        │         │                          │
   │  - Furniture: $Y          │         │  Result:                 │
   │  - Stationery: $Z         │         │  - North: avg $X         │
   └─────────┬─────────────────┘         │  - South: avg $Y         │
             │                           │  - East:  avg $Z         │
             │                           │  - West:  avg $W         │
             │                           └────────┬─────────────────┘
             │                                    │
             ├────────────────────────────────────┤
             │                                    │
             ▼                                    ▼
   ┌─────────────────────────────────────────────────────┐
   │   Write Outputs (4 separate files)                  │
   │                                                      │
   │  1. transformed_data/     - Filtered transactions   │
   │  2. sales_by_category/    - Aggregated by category  │
   │  3. avg_by_region/        - Aggregated by region    │
   │  4. top_customers/        - Top 5 customers         │
   │                                                      │
   │  Format: CSV with headers                           │
   │  Mode: Overwrite                                    │
   └──────────────────┬──────────────────────────────────┘
                      │
                      ▼
           ┌──────────────────┐
           │  Output Files    │
           │  (data/output/)  │
           └──────────────────┘
```

## Component Interaction Diagram

```
┌─────────────┐
│    User     │
└──────┬──────┘
       │
       │ (1) Trigger DAG
       │
       ▼
┌─────────────────────┐
│  Airflow Webserver  │  (Port 8080)
│  (Flask App)        │
└──────┬──────────────┘
       │
       │ (2) Send trigger request
       │
       ▼
┌─────────────────────┐
│ Airflow Scheduler   │
│ (Background Process)│
└──────┬──────────────┘
       │
       │ (3) Schedule tasks
       │
       ▼
┌─────────────────────┐
│  Task Queue         │
│  (SQLite DB)        │
└──────┬──────────────┘
       │
       │ (4) Execute tasks
       │
       ├──────────────────────────────────────┐
       │                                      │
       ▼                                      ▼
┌─────────────────┐              ┌──────────────────────┐
│  PythonOperator │              │   BashOperator       │
│  (Validation)   │              │   (Run Spark)        │
└─────────┬───────┘              └──────────┬───────────┘
          │                                 │
          │ (5) Check files                │ (6) Execute command
          │                                 │
          ▼                                 ▼
    ┌──────────┐                  ┌────────────────────┐
    │   Pass   │                  │  python3 spark_    │
    └──────────┘                  │  jobs/hello_world_ │
                                  │  spark.py          │
                                  └─────────┬──────────┘
                                            │
                                            │ (7) Create session
                                            │
                                            ▼
                                  ┌────────────────────┐
                                  │  SparkSession      │
                                  │  (local[*] mode)   │
                                  └─────────┬──────────┘
                                            │
                                            │ (8) Process data
                                            │
                                            ▼
                                  ┌────────────────────┐
                                  │  Read → Transform  │
                                  │  Aggregate → Write │
                                  └─────────┬──────────┘
                                            │
                                            │ (9) Write files
                                            │
                                            ▼
                                  ┌────────────────────┐
                                  │  data/output/      │
                                  │  (CSV files)       │
                                  └────────────────────┘
```

## Technology Stack Layers

```
┌──────────────────────────────────────────────────────┐
│                   User Interface                      │
│  - Airflow Web UI (http://localhost:8080)            │
│  - Spark UI (http://localhost:4040)                  │
└──────────────────────────────────────────────────────┘
                        │
                        ▼
┌──────────────────────────────────────────────────────┐
│               Orchestration Layer                     │
│  - Apache Airflow 2.9.3                              │
│    • DAG definition and scheduling                   │
│    • Task execution and monitoring                   │
│    • Error handling and retries                      │
└──────────────────────────────────────────────────────┘
                        │
                        ▼
┌──────────────────────────────────────────────────────┐
│              Processing Layer                         │
│  - Apache Spark 3.5.1                                │
│    • Distributed data processing                     │
│    • DataFrame API & Spark SQL                       │
│    • In-memory computation                           │
└──────────────────────────────────────────────────────┘
                        │
                        ▼
┌──────────────────────────────────────────────────────┐
│               Application Layer                       │
│  - Python 3.9-3.11                                   │
│  - PySpark API                                       │
│  - Custom business logic                             │
└──────────────────────────────────────────────────────┘
                        │
                        ▼
┌──────────────────────────────────────────────────────┐
│                Storage Layer                          │
│  - SQLite (Airflow metadata)                         │
│  - CSV files (Input/Output data)                     │
│  - Local file system                                 │
└──────────────────────────────────────────────────────┘
                        │
                        ▼
┌──────────────────────────────────────────────────────┐
│            Infrastructure Layer                       │
│  - Local Machine (macOS/Linux/Windows)               │
│  - JVM (Java Virtual Machine)                        │
│  - Python Virtual Environment (UV)                   │
└──────────────────────────────────────────────────────┘
```

## Deployment Model

```
Development Setup (Local Machine)
─────────────────────────────────

┌─────────────────────────────────────────────────────┐
│  Environment: Local Development                      │
│  Executor: SequentialExecutor                       │
│  Database: SQLite                                    │
│  Spark Mode: local[*]                                │
│  Parallelism: 4                                      │
└─────────────────────────────────────────────────────┘

Future Production Setup (Example)
──────────────────────────────────

┌─────────────────────────────────────────────────────┐
│  Environment: Cloud (AWS/GCP/Azure)                  │
│  Executor: CeleryExecutor or KubernetesExecutor      │
│  Database: PostgreSQL/MySQL                          │
│  Spark Mode: YARN/Kubernetes/Standalone cluster      │
│  Parallelism: Configurable based on resources        │
└─────────────────────────────────────────────────────┘
```

## File Structure Visualization

```
SPARK_AIRFLOW_GENAI/
│
├── 📘 Documentation Files
│   ├── README.md              (Main documentation)
│   ├── QUICKSTART.md          (Quick reference)
│   ├── GETTING_STARTED.md     (Setup checklist)
│   ├── PROJECT_SUMMARY.md     (Project overview)
│   ├── CONTRIBUTING.md        (Contribution guide)
│   ├── CHANGELOG.md           (Version history)
│   └── LICENSE                (MIT License)
│
├── ⚙️ Configuration
│   ├── pyproject.toml         (Python dependencies)
│   ├── .env.example           (Environment template)
│   ├── .gitignore             (Git ignore rules)
│   └── config/
│       ├── airflow.cfg        (Airflow settings)
│       ├── spark.conf         (Spark settings)
│       └── log4j.properties   (Logging config)
│
├── 🔧 Automation Scripts
│   └── scripts/
│       ├── setup.sh           (Complete setup)
│       ├── start_airflow.sh   (Start services)
│       ├── stop_airflow.sh    (Stop services)
│       └── run_spark_job.sh   (Run job directly)
│
├── 🌊 Airflow Components
│   └── dags/
│       └── hello_world_dag.py (Main DAG)
│
├── ⚡ Spark Components
│   └── spark_jobs/
│       ├── __init__.py
│       └── hello_world_spark.py (Data processing)
│
├── 📊 Data
│   └── data/
│       ├── input/
│       │   └── sample_sales_data.csv
│       └── output/            (Generated files)
│
└── 🧪 Testing
    └── tests/
        ├── __init__.py
        ├── conftest.py
        ├── test_spark_job.py  (12 tests)
        └── test_dag.py        (10 tests)
```
